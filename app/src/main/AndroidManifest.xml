<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kkphim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KKPhim"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="screenSize|smallestScreenSize|density|fontScale"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".ui.FilterActivity"
            android:exported="false"
            android:configChanges="screenSize|smallestScreenSize|density|fontScale" />

        <activity 
            android:name=".ui.DetailActivity"
            android:exported="false"
            android:configChanges="screenSize|smallestScreenSize|density|fontScale" />

        <activity
            android:name=".ui.PlayerActivity"
            android:exported="false"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.KKPhim.Player"
            android:configChanges="uiMode|orientation|screenSize|layoutDirection|keyboardHidden|smallestScreenSize|screenLayout|density|fontScale" />

    </application>
</manifest>
